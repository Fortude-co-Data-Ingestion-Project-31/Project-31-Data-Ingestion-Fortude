// Frontend tests go here (e.g., Jest/RTL). This placeholder indicates
// where to add React unit/integration tests. Run frontend tests with
// the project's chosen test runner (not included by default).
